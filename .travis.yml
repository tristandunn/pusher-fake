sudo: false
cache: bundler
language: ruby

rvm:
  - 2.3.8
  - 2.4.6
  - 2.5.5
  - 2.6.3

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"
  - "sleep 5"

script:
  - bundle exec rspec
  - bundle exec rubocop
